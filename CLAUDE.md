# บึ้งไทย 3D — project notes for Claude

Browser game: realistic Thai tarantula terrarium simulator. Thai UI. No build step, no npm deps.
Owner prefers: simple Thai explanations, short answers first, plan before big changes, minimal effort / max ROI.
Whenever you use a helper (subagent, skill, or automated tool like code-review), tell the owner which one and, in one short line, what it does.

## Run / publish
- Open `index.html` directly (file://) or via GitHub Pages. three.js **r128** classic scripts (globals), vendored in `lib/`.
- Live link (claude.ai artifact, update in place): https://claude.ai/artifact/KDDtfiat4ynKSTrThpS7zp
  Republish = copy `index.html js/ lib/` to a scratch folder and publish with all js/lib files listed in `files`.
- Commit straight to `main`. End commits with the session attribution lines.

## Tank + city (kaiju mode)
- Tank `TW=120, TD=80, TH=56`. The old hand-placed 60×40 garden (log, rocks, dish, moss, ferns, plants) is mapped by `nat(x, z)` (scale `NS=1.3`) into the front-left; `inCity(x, z)` = back strip (z < -13) + right strip (x > 19) holds the ruined Japanese town (`js/city.js`).
- Buildings are walkable like rocks: game.js turns every `CITY.buildings[i].solid` into a height grid (`heightGrid()`, shared with ROCKS) in `SOLIDS`; `groundY` includes them. A building taller than `CLIMB (.35) × span` is a wall (steering + hard push in `drive()`/tick, `solidNear()`), lower ones the spider steps onto. Leg joints are pushed sideways out of walls (`solidPush()` in unclip/solve), a foot left behind a wall re-steps (`legBlocked()`), body rises early/at a capped rate on a roof edge.
- Spider span 5 at start, ×1.35 per molt up to `maxSpan × KAIJU (2.4)` (31–38, 6–7 molts). Speed grows sub-linearly; prey scale `p.k = span/12` (1–3.5; size, speed, food value). Bigger than `LOG_FIT (22)` it no longer hides in the log.
- Back wall (was cork): `city.js` builds a mossy retaining wall (擁壁) + ruined facades against the back glass, inside the merged BODY/DECO/MOSS meshes (no extra draw calls, not solid). Lid fixtures (`ledBar`, lamp hood, bulb) are hidden; their lights + god rays stay.
- Lots added in round 4 (all low, checked with a flood-fill for spans 5–38 so every street stays reachable): 2nd `store` (-6, -11), sheds (-30, -12.5) (-56, -12.8) (-53, 36.5), kiosks (-40, 27.5) (16, 37). A tall lot in front of the back street pinches it shut for spans 15–22 (the log blocks the detour) — keep new lots there low.
- Film cam (#tCine): street level, looks up at the spider, swings/pulls in until nothing blocks the view; fog ×2.4 and light shafts in the street gaps (`cityBeamMat`, in `beams`).

## Files (all share global scope, load order matters)
- `index.html` — markup, CSS, script tags. UI = dark glass: compact stats card `#hud`, bottom bar with 4 main buttons `#tCricket #tDubia #tMist #tFollow` + `#tLogBtn` (log, unread badge) + `#tSettings` (drawer `#drawer` holds all other buttons, same ids). Body classes `setOpen`/`logOpen` open one panel at a time; `idle` fades UI after 5 s without input.
- Load order: intro → three/lib → `lots.js` → `world.js` → `city2.js` → `city.js` → `spider.js` → `prey.js` → `human.js` → `survive.js` → facts/voice → `game.js` → `look.js` → `pond.js` → `webs.js` → `heli.js` → `sound.js` → `perf.js` (last).
- `js/lots.js` — town plan `LOTS` (data only, loaded before world.js) + `lotInside(x, z, pad)`; `clearSpot` uses it until game.js sets `cityInside`. New lots: append here (kinds from city.js KIND or city2.js).
- `js/city2.js` — `CITY_EXT(kit)`: extra building kinds (husk, yard, mound, crushed, fence, rowx, kiosk, corner, lantern — ruins in the garden, `garden: 1`) built with city.js's builder kit (`KIT` = face/wall/box/lump/mossAlong/windowAt/...); `decorate()` adds flat slabs only (not solid). Footprint w×d must enclose everything walkable (grid trimmed to it).
- `js/prey.js` — `class Prey` (moved out of game.js): behaviour, tripod gait, burrow, human flee/hide/tire (`human()`); `PREY_KINDS[kind](p, g)` builds a look; `LimbBatch` + `limbJoints()` = all moving limbs re-posed on the CPU into ONE mesh (dubia = 2 meshes: shell + limbs, female plated / 40% winged male, canvas texture).
- `js/human.js` — ชัยภัทร (178 cm = 1.78 units: red graduation gown with gold bands/black+gold cuffs, white shirt, black trousers, curtain-fringe hair; medium-poly head, `HUMAN_GLASSES` flag; gown hem on its own swaying joint `J.hem`; walk 1.4 / sprint 7.5 then tires, hides behind buildings, speech bubble `humanSay`/`preyTalk`), `BLOOD` FX (splash / drip / stain fading ~45 s / silk `cocoon`). Button `#tHuman` in the drawer, enabled at span ≥ `HUMAN_SPAN` (12).
- `js/pond.js` — looks of the water hole (murky water shader patch on `water`, wet bank, moss, stones, reeds, floating leaves).
- `js/webs.js` — decorative seeded spider webs (~218: orb / sheet-funnel / cobweb tangles / hanging threads + twinkling dew; fibre sheen + backlight glow, neon tint at night, sway on `TURF_U.uTime`), 3 draw calls, anchors found by ray casting the solid scene at load (~0.5 s). Quality = draw range (high all, fast 12%, min 8%: fast mode stays as quick as before). Hidden from the DOF depth pass by its own `bokeh.render` wrap; hooks `setMeadowDensity` and `CITY.update`.
- `js/heli.js` — `HELI` (+ `HELI2`, both from `makeHeli`): low-poly rescue helicopter (1 vertex-coloured hull + 2 rotor meshes + faint rotor disc) circling the town on an ellipse at y≈43, blinking beacon/strobe, searchlight cone (`beamMat`, in `beams`) + additive light pool; the light follows a chase on ชัยภัทร. `HELI.rescue(p, done)` = descend/hover/`done()`/leave out of the lid. `FIRE`: one small particle fire (flame + smoke Points, custom shader) at the garden husk's torn-down corner, only on quality high. `FX_UPDATE(dt, night, chase)` from game.js loop; `FX_HIDE` kept out of the DOF depth pass.
- First-person button `#tEyes` (drawer, กล้อง): camera at ชัยภัทร's eyes (neck joint `localToWorld`), set after `controls.update()`, controls disabled, fov ≥ 66; auto-off when he is gone.
- `js/look.js` — loaded after game.js: art layer over world.js (plants, grass `GRASS` ≤4.5k blades at town scale (0.4–1.9, a few ~2.5) concentrated at wall feet, curbs/street cracks, rock/log edges, rubble tops, thinner meadow; per-blade priority `p` → low modes drop meadow first, moss, decor, rock + cork-wall textures `CORKWALL`) and `contactShadows()` (instanced soft shadows under body, leg tips, prey; called from `loop`). Grade pass also has lens fringe `uCA`.
- `js/world.js` — renderer, procedural PBR textures (canvas → albedo + normal; `tileFbm` = seamless noise), terrain `groundY/groundN/soilY`. Rocks (`ROCKS`) are real meshes (plane-cut icospheres + cracks, triplanar shader `triplanar()`), each with a top-down height grid rasterised from its mesh (`k.grid`, `gridY`) → the walkable surface = the visible one. Water hole (replaced the stone dish): `dishPos` centre, `pondR(a)` shore radius, `pondT/inPond`, `WATER_Y`, basin dug into `soilY` (walkable floor, spider wades, prey avoid via `preyObs`, splash rings `spawnRipple(p, s, true)`). Hollow log hide (`LOG`, `BURROW`, `LOG_ENTRY`, `underLog`), moss shells, ferns, bendable foliage (`addFoliage`, `updateFoliage(dt, spheres)`: sedges, strap-leaf clumps (only every 3rd `plantSites` site), round-leaf rosettes; `gpuWind` = wind waves in the vertex shader (`TURF_U`) for the grass, CPU only updates clumps something touches), leaves, pebbles, lights (`led`, `lamp`, `hemi`, `moon`, `rim`), god rays (`beams` group: 5 additive sheets under `ledBar` + open cone under lamp, `beamMat`, `ledBeamMat`/`lampBeamMat` `uI`, `BEAM_U.uTime`; edge-on fade via facing term), soft round additive `dust` (1500), `applyEnv`, collision sets (`obstacles`, `preyObs`), helpers (`rand clamp lerp frac gauss fbm sstep smin seeded track onRock clearSpot`).
- `js/spider.js` — `SPECIES`, `class Spider`: shell fur + animated hair ribbons (`HAIR_U` shader uniforms), 4-segment IK legs, metachronal tetrapod gait, spring abdomen, fangs/palps, `exuvia()`. Terrain-safe legs: `foothold()` picks reachable, unobstructed footholds; distal segments follow the ground normal; `unclip()` (FABRIK) pushes joints out of rock/soil; body floor from `BODY_UNDER` + hips. API used by game: `pos yaw vel yawRate mode modeT route prey want{threat,rear,stalk,fang,flip,hidden,eat} soft span sp root worldOf update placeFeet dispose exuvia pickables colliders`.
- `js/city.js` — loaded right after world.js: procedural mossy Japanese back-street town (merged meshes per material, canvas atlas for windows/signs). Exports `CITY = { buildings: [{ x, z, w, d, rot, h, kind, solid }], inside(x, z, pad), meshes, update(dt, night) }`; game.js sets `cityInside` (clearSpot keeps grass/prey/wander targets out) and calls `CITY.update` each frame (window glow, neon flicker).
- `js/intro.js` — 10 s hand-drawn 2D intro (Canvas 2D, paper + ink style) shown once per session (`sessionStorage` key `tarantula3d-intro`) before the name/species card; loaded before three.js. Skip / เริ่มเล่น fade into the game. Fast quality = no grain, line boil, glow. Test hook `INTRO.draw(seconds)`.
- `js/facts.js` — `FACTS` (Thai, source-checked; refs in README).
- `js/voice.js` — `VOICE`: spider's speech lines by category (mood/situation + chuunibyou Spider-Man `chatter`), ~37 lines.
- `js/game.js` — calls `updateFoliage` each frame with spider + prey spheres; state `S` (hunger, temp, hum, growth, molt phases normal→premolt→molting→soft), behaviour state machine, path planning around the log, prey (cricket, dubia: jointed `limb()` chains, tripod gait, smooth turning `face`→`yaw`, antennae sweep, cricket crouch→kick jump + night chirp, dubia digs in/out, bitten prey struggles), speech `say(cat, pri)` + `moodTalk()` (bubble `#say` over spider + log), log stamped with real device time, auto-care once per crisis (`S.autoFed` hunger≥85, `S.autoMist` hum<55), wander in stop-and-go bursts, vibration ripples, god-ray strength = ledK/lampK × S.hum × `haze` (mistFx sets 1, decays ~40 s) × more at night, hidden on quality 'min'; cinematic cam `cine`/`setCine` (#tCine: low at span*.35, dist span*1.6, fov≥50, relaxes maxPolarAngle, auto-off with tankView/saver), post-processing (Render → Bokeh DOF → UnrealBloom → grade → FXAA; `quality` high/low), HUD, save (`localStorage` key `tarantula3d-v2`), main loop.

## Navigation (round 4)
- Spider: `navGrid(L)` = 1-unit grid of cells a spider of span L can stand in (walls(L) inflated by .32 L, log box, tank edge) + connected areas (`comp`); `astar()` (8-neighbour, string-pulled); `cityRoute()` swaps any route leg that hits a wall for an A* path; `routeTo(to)` = planRoute (log) + cityRoute. `follow_route` watchdog: no progress 1.3 s → `unstick()` backs off the nearest wall (.7 s) and re-routes with forced A*; 2nd time drops the waypoint, >3 gives up. Arrival radius grows with span. Wander targets pass `goodSpot()` (free, same area, ≥ .36 L from tall walls).
- Prey: `preyBlocked()` / `openDir(p, sp, flee)` (16 headings: open ground up to 7 vs away-from-spider, avoids the heading it got stuck on). Watchdog in `Prey.update`: wants to move but moved < 30% in 1.2 s → turn out via openDir and hold it 1.5 s. Dubia/cricket flee use openDir.
- Test scripts (scratchpad, not in repo): flood-fill reach per span, headless stuck counters (spider 4 sizes × 300 s: 16 → 7; prey 12 × 180 s ×2: 156 → 35).

## Survival round (round 4, `js/survive.js` + game.js `CHASE`)
- Releasing ชัยภัทร starts `ROUND` (1 survival day = `DAY_S` 12 s × TM, `ROUND_DAYS` 30). Day 30 → `HELI.rescue` (he hides + keeps still, heli descends, `p.boarded`, win line). Eaten → lose line. Log only; bubbles for the rest (`humanSay` LOGGED set).
- `humanAI`: states hide / peek (ducks back if it sees/feels the spider) / trip (random nearest store = store/kiosk/食堂, or pond bank; detours when not urgent; mostly by day) / act / flee / wait. Human grid `humanNav()` (strips behind the back + east rows closed), `covered()` line-of-sight, `humanRoute()` avoids the spider. Needs `HUM.eatDays` 4 / `drinkDays` 3, at 0 only slower (×.8) + lower max stamina. Sprint 9.5 only when really hunted (stamina `sprintS` 6.5 s), jukes + a last-moment dodge on the lunge (`HUM.dodge`).
- Spider: hunts him when his steps are felt (radius `senseBase + L·senseL` × gait vib walk .6/jog 1/sprint 1.5, ×1.3 night) and hunger > `hungerD`/`hungerN` (day/night); prefers nearer insects. Chase: top = sprint × `overHuman`, slow `acc`, wide `turn`, A* round buildings, rushes to where he went quiet and feels around (`creep`, leg reach `touch`). Starving (hunger > 72, no other food): senses/runs/accelerates more (`starve*`), camps. Chase clock ends every chase in `tMin..tMax` (+2 starving) s → spider rests.
- Balance (headless, span 20, 60 rounds each, harness in scratchpad): keeper feeds → 63% survive; never feeds → 35%; chase median 11–13 s. Very sensitive to `hungerD/N` (45/29 → 57%, 48/32 → 88% fed).

## Round 5 (bubbles, spawn, 2nd heli)
- Bubbles: off screen = hidden (no pinning to the edge). ชัยภัทร's bubble hides when a roof is between camera and head (`sayBlocked`, 6×/s) or camera > 95 away; per-kind wait `GAP` in `humanSay` (tired 14 s, juke 7 s…); if it overlaps the spider's bubble it goes under his head.
- Spawn: `HUMAN_SPAWNS` in survive.js (back street (-18, -19), right street (33, 12)); `humanSpawn()` takes the one farthest from the spider, snapped to a free main-area `humanNav` cell. He waits 5–7 s before hiding; `focusOn(p)` glides the camera to him 3.5 s (skipped in cine/eyes/tank/saver; pressing the button again while he is out re-focuses). He does not count toward the 4-prey limit; log says why a release is refused.
- `makeHeli(o)` builds each helicopter: `HELI` (red, rescue) + `HELI2` (blue, patrol only, y 33 lower loop, other direction, climbs to TH-6 while HELI is busy; its searchlight only on high).

## Scale + climbing
- World scale: a storey ≈ 3 units, a person ≈ 1.7; grass/leaves/twigs/sedges sized to that (big rocks kept as they were — changing them raised leg clipping before).
- Climbing (spider.js `poseRoot`/`foothold`): body height follows its target at a capped speed (≤ ~1.4 span/s up, 0.9 down → ≤ 0.05 span per frame), nose-up pitch at a roof edge ahead / nose-down going down (`fA − bA`, total pitch clamped), `climbLag` slows `drive()` while the body catches up; footholds far above/below the hip are pulled in (`rEff`, below-hip penalty) so knees keep their arch; hind feet stay on a roof until the hips pass the edge (SOLIDS only, not rocks).

## Round 6 (two people, endless rounds, sound)
- Log `#log` = right side panel (320px, slides in .25 s, stops above the bottom bar; < 600px: bottom half, full width). Short centre notice `notice(text)` (`#notice`).
- Camera lock `#tView` (drawer, keys 1–5, 0 = free): `view` front/right/back/left (25° down, whole tank) / top; `viewCam(dt)` glides .6 s, rotation off, zoom on; side views pan with Follow. Exclusive with cine / eyes / tankView; `focusOn` ignored while locked.
- Old molt skins (`exuviae`): 3 min (`EXU_LIFE`, × fast-forward), then fade + sink over 10 s, `dropExuvia()` disposes. Max 2 kept.
- People (`PEOPLE` in survive.js): ชัยภัทร `chai` + ตุ้ย `tui` (170 cm, `J.hold` scaled; slim school-shirt build `HGEO.build('tui')`, spiky hair via `headGeo({ hair: 'spiky' })`, code-made face; arms crossed while waiting in cover; scaredy-cat numbers hideK 1.2 / peekK .7 / startleK 1.2, `nervous` = faster bites; own chuunibyou-coder lines `TUI` in `humanSay`). `new Prey('human', who, at)`; `p.name` everywhere. The photo face is NOT in the game (blocked in session; see git log).
- `ROUND` (survive.js): `begin()` releases both 2 s apart (`pend` queue, retries every 1 s while the spider is too close), `ROUND.p` = list. Eaten → same person back after 10 s (`enter(who, true)`), day count keeps going; the other says a grief line and keeps still 8–12 s (`grief`). Day 30: HELI + HELI2 pick one each, no respawns after that. Stars by deaths (0 → 3, 1 → 2, 2+ → 1); 15 s break then `begin()` again. Drawer toggle `#tAutoRound` (`S.autoRound`, default on; off = one round, no respawn). `S.rounds` / `S.best` (old saves migrated in `begin()`).
- Random spawn `humanSpawn(others)`: random free main-area humanNav cell ≥ 25 from the spider, ≥ 15 from the other person, not pond / under log; area name from `spawnArea()`; 50 misses → old fixed spot; `.ok` false = retry.
- Spider picks the person felt most (distance ÷ vibration); the one not being chased only says caught / rescue / grief / respawn lines. Hide spots ≥ 6 apart (`hideTaken`). Bubbles: follow the head with a lerp, overlaps nudge up/down smoothly (no side jumps; the panic shake animation was removed).
- Eating / drinking (human.js, `p.acting` set by humanAI in 'act', 3.4 / 3.2 s): onigiri in the right hand (in the LimbBatch, shrunk to 0 when unused), bite ~0.8 s + chew nod + glances; pond: crouch + cupped hands. Leaving 'act' early = snack dropped.
- Balance: `CHASE.meal` 16 (a person's food value, was 55), `CHASE.pair` ×1.4 sense while 2 people move, hungerD/N 33/22, overHuman 1.5 (chases used to time out just short of a catch), biteR .28, senseBase 15, `HUM.dodge` .2, `sprintS` 5.5, eatDays/drinkDays 3/2. Auto-feed ignores people. Headless (span 20, keeper feeds, 12 rounds): deaths 0/1/2 = 3/7/2 rounds → ⭐⭐⭐ 25% / ⭐⭐ 58% / ⭐ 17%, chase median 11.1 s.
- Camera `#tWatch` (drawer): follows one person by `who` (off → ชัยภัทร → ตุ้ย → off), picks him up again after a respawn; exclusive with follow / cine / eyes / tank / top view.
- `js/sound.js` (`SOUND`): WebAudio only — room rumble + mains hum, wind (LFO), spider footsteps (noise tap, lower + thump when big), helicopter rotor (chopped noise, by distance). Starts on first click/key; `#tSound` / `S.sound`; suspended while the tab is hidden. Hooks `FX_UPDATE`.

## Performance (round 7)
- Measured (headless 1280×720, quality high, span 20; per frame incl. 3 shadow maps + DOF depth + post): draw calls 1326 → 336 by day / 1327 → 289 at night; triangles 4.86M → 3.45M / 3.02M; scene meshes 397 → 257. CPU per frame without rendering (2 people out, 600 frames) 6.6 → 2.1 ms (foliage 5.2 → 0.5). Look unchanged (deterministic A/B screenshots: only the wall-clock-driven helicopters differ).
- `js/perf.js` (loaded last): merges `STATIC_BATCH` meshes (world.js pushes rocks, moss-mound layers, fern stems, tank frame bars) that share material + shadow flags into one mesh each (−142 meshes); must run after webs.js (it ray casts the separate fern stems). Also pre-builds `humanNav()` and `BLOOD.warm()` at load (no hitch on the first release).
- Spider legs (`batchLegs()` in spider.js): the 66 world-space segments + knobs and their 38 hair meshes are drawn as 2 `SkinnedMesh` (bone = the original mesh, now invisible). IK, `pickables()` (ray casts ignore visibility) and `exuvia()` (clones set visible) still use the originals. `mats.leg` is skinned-only; the coxae/spinnerets on the body use a plain copy (in `tint`/`ownMats`/`envMats`). Needs float vertex textures, else falls back to separate meshes. `dispose()` removes `skinned` + `skel`.
- DOF (game.js `bokeh.render`): the depth target is only 2×2 (BokehPass r128 has no setSize) → the depth scene pass runs every `DOF_EVERY` (3) frames; blur every frame. (Making the DOF a real per-pixel depth would change the look — not done.)
- Shadows: `SHADOW_LIGHTS` with `intensity ≤ .002` (sun at night, LED/lamp off) skip their shadow-map render (`shadow.autoUpdate = false`, `needsUpdate` per frame).
- Moss puffs (look.js): buried lower faces dropped + indexed geometry with `flatShading` (same facets, ~5× fewer vertex runs). The fuzz rim uses `normal` (not `vNormal`).
- `updateFoliage`: per clump only the colliders that can reach it (`cl.reach`, `_fnear`) are tested — the old global box touched almost every clump when prey/people were spread out (5 ms → 0.5 ms).
- `preyTalk`: DOM reads first, style writes at the end (no forced layout per bubble).
- Dynamic resolution + auto quality (`DRS`, `autoQuality(raw)`, `applyRes()`, `setQuality(q)`): 1 s windows; < 50 fps → render scale ×.85 down to the mode floor (`prFloor`: high 1 on HiDPI / .8, low .7, min .6); ≥ 57 fps for 3 s → back up (a scale that just failed is capped for 30 s); at the floor and still < 42 fps for 3 s → high → fast, < 26 fps → lowest (with a log line). A manual `#tQual` pick stops automatic mode drops (scaling stays). Harness stubs `autoQuality = () => {}` as before.
- Ideas not done: real per-pixel DOF at ½ res (look change), spider body fur shells/hair (~40 draw calls) still separate, `scene.updateMatrixWorld` still walks every object.

## Science rules baked in
Vibration sensing (no good eyesight), external digestion, premolt fasting + dark abdomen, molt on back, soft pale fangs after molt (no feeding), low humidity → hard molt, live prey dangerous during molt, ectotherm speed ∝ temperature, nocturnal & light-avoiding, Old World = no urticating hairs, threat posture. Names: *Melopoeus minax*, *Chilobrachys huahini*, *Melopoeus lividus* (WSC 2026).

## Testing
Harness pattern (kept outside the repo): Playwright + `executablePath: '/opt/pw-browsers/chromium'`, init script sets `sessionStorage['tarantula3d-intro']`, click `goBtn`, `window.requestAnimationFrame = () => 0`, `autoQuality = () => {}`; call `canvas.toDataURL()` in the SAME evaluate right after `loop()` (a later call returns black). Leg clipping: for joints inside a building footprint measure horizontal depth, not `groundY − y` (the grid edge reports roof height right at the wall face).
Container has no GPU: Chromium/Playwright uses SwiftShader (very slow, ~30 s load). Use `page.evaluate(() => document.getElementById('goBtn').click())` instead of `page.click`, long timeouts, collect `pageerror`. Syntax check: `node -e "new Function(require('fs').readFileSync('js/game.js','utf8'))"`.

## Testing tips
Render on demand: after load set `window.requestAnimationFrame = () => 0`, step `tick(1/30)` + `updateFoliage(...)` yourself, then `loop(); canvas.toDataURL()` (page.screenshot times out under SwiftShader). Leg clipping check: walk routes across rocks and measure how far `l.J` joints/segment middles are inside `groundY` (≈0 now for span 9 and 15).

## Known open items
Big spiders (≥ 31) can still dip a leg ~1–2 into thin low garden walls / rocks (~5% of span). No true vertical wall-climbing: tall buildings are walls, the spider only steps onto roofs lower than 0.35 × span (a wall-walking gait would be a new system). Log interior a bit dark; ferns slightly stylised (sway in wind + lean from spider body via shader, not per-leaf physics); big spiders' legs can clip log walls inside the tunnel; no sound yet.
